version: "3.9"
services:
  db:
    image: postgres:15
    container_name: renderis_bot_db
    environment:
      POSTGRES_DB: renderis_bot
      POSTGRES_USER: bot
      POSTGRES_PASSWORD: bot
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
